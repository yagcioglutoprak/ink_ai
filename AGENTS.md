# Agent Instructions

After every file change or set of related changes, run:

```
git add -A && git commit -m "<short descriptive message>"
```

Do this consistently — after each phase, after each component, after each meaningful edit. Never accumulate uncommitted changes.
